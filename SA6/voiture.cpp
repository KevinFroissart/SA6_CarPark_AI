#include "voiture.h"
#include <iostream>
#include <thread>
#include <fstream> 
using namespace std;

/*Voiture::Voiture(int id, string filePath) : m_id(id), m_path(filePath){
    

}*/

Voiture::Voiture(int id, string filePath) : m_id(id), m_path(filePath){


}